{
	"name": "utilitypes-ts",
	"version": "1.0.0",
	"description": "Provides TypeScript utility types",
	"scripts": {
		"test": "vitest --run",
		"prepare": "npx husky",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"husky": "^9.1.7",
		"prettier": "^3.8.1",
		"typescript": "^6.0.2",
		"vitest": "^4.0.18"
	}
}
